
# Universal Variable Window Template

"""
left = 0
for right in range(len(arr)):
    # include arr[right] into window

    while window is invalid:
        # remove arr[left]
        left += 1
"""
    # update answer

"""
Variable Window Pattern (CORE LOGIC)
3 Steps:

Expand window → move right

Fix violation → move left

Update answer
"""